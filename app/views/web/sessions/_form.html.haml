= simple_form_for @type, url: session_path(from: params[:from]), html: { class: 'form-horizontal' } do |f|
  = f.input :email
  = f.input :password

  .form-actions
    = f.button :submit, class: 'btn btn-success'
